language: go

sudo: required
dist: trusty

go:
  - "1.7"

install:
  - mkdir -p $GOPATH/bin
  - curl https://glide.sh/get | sh
  - glide install

script:
  - go test -v $(glide novendor)

after_success:
  - bash <(curl -s https://codecov.io/bash)